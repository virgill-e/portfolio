function flip () {
    //todo:Lors du ckick  sur container la div card va tourner en recevent ou en perdant la classe flipped
    var element = document.getElementById("flip");
    element.classList.toggle("flipped");
}

